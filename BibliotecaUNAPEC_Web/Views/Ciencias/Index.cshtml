@model IEnumerable<BibliotecaUNAPEC_Web.Models.Ciencia>

@{
    ViewData["Title"] = "Mantenimiento de Ciencias";
}

<div class="container mt-4">
    <div class="d-flex justify-content-between align-items-center mb-4">
        <h2 class="fw-bold text-primary">Mantenimiento de Ciencias / Áreas</h2>
        <a asp-action="Create" class="btn btn-primary shadow-sm">Registrar Nueva Ciencia</a>
    </div>

    <div class="card shadow-sm border-0">
        <div class="card-body p-0">
            <div class="table-responsive">
                <table class="table table-hover table-striped mb-0">
                    <thead class="table-dark">
                        <tr>
                            <th>Descripción / Área del Conocimiento</th>
                            <th>Estado</th>
                            <th class="text-center">Acciones</th>
                        </tr>
                    </thead>
                    <tbody>
                        @foreach (var item in Model)
                        {
                            <tr>
                                <td class="align-middle fw-bold">@item.Descripcion</td>
                                <td class="align-middle">
                                    @if (item.Estado == true)
                                    {
                                        <span class="badge bg-success py-2 px-3">Activa</span>
                                    }
                                    else
                                    {
                                        <span class="badge bg-danger py-2 px-3">Inactiva</span>
                                    }
                                </td>
                                <td class="align-middle text-center">
                                    <div class="btn-group" role="group">
                                        <a asp-action="Edit" asp-route-id="@item.IdCiencia" class="btn btn-sm btn-outline-primary">Editar</a>
                                        <a asp-action="Details" asp-route-id="@item.IdCiencia" class="btn btn-sm btn-outline-secondary">Detalles</a>
                                        <a asp-action="Delete" asp-route-id="@item.IdCiencia" class="btn btn-sm btn-outline-danger">Borrar</a>
                                    </div>
                                </td>
                            </tr>
                        }
                    </tbody>
                </table>
            </div>
        </div>
    </div>
</div>