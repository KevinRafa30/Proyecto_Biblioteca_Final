@model IEnumerable<BibliotecaUNAPEC_Web.Models.Idioma>

@{
    ViewData["Title"] = "Mantenimiento de Idiomas";
}

<div class="container mt-4">
    <div class="d-flex justify-content-between align-items-center mb-4">
        <h2 class="fw-bold text-primary">Mantenimiento de Idiomas</h2>
        <a asp-action="Create" class="btn btn-primary shadow-sm px-4 fw-bold">Registrar Nuevo Idioma</a>
    </div>

    <div class="card shadow-sm border-0">
        <div class="card-body p-0">
            <div class="table-responsive">
                <table class="table table-hover table-striped mb-0">
                    <thead class="table-dark">
                        <tr>
                            <th class="ps-4">Descripción / Idioma</th>
                            <th>Estado</th>
                            <th class="text-center">Acciones</th>
                        </tr>
                    </thead>
                    <tbody>
                        @foreach (var item in Model)
                        {
                            <tr>
                                <td class="align-middle fw-bold ps-4">@item.Descripcion</td>
                                <td class="align-middle">
                                    @if (item.Estado == true)
                                    {
                                        <span class="badge bg-success py-2 px-3">Activo</span>
                                    }
                                    else
                                    {
                                        <span class="badge bg-danger py-2 px-3">Inactivo</span>
                                    }
                                </td>
                                <td class="align-middle text-center">
                                    <div class="btn-group" role="group">
                                        <a asp-action="Edit" asp-route-id="@item.IdIdioma" class="btn btn-sm btn-outline-primary px-3">Editar</a>
                                        <a asp-action="Details" asp-route-id="@item.IdIdioma" class="btn btn-sm btn-outline-secondary px-3">Detalles</a>
                                        <a asp-action="Delete" asp-route-id="@item.IdIdioma" class="btn btn-sm btn-outline-danger px-3">Borrar</a>
                                    </div>
                                </td>
                            </tr>
                        }
                    </tbody>
                </table>
            </div>
        </div>
    </div>
</div>